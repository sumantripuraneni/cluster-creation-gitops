apiVersion: v1
kind: Service
metadata:
  name: {{ include "standard-ocp-build.fullname" . }}
  labels:
    {{- include "standard-ocp-build.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "standard-ocp-build.selectorLabels" . | nindent 4 }}
