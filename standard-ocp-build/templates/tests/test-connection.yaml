apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "standard-ocp-build.fullname" . }}-test-connection"
  labels:
    {{- include "standard-ocp-build.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "standard-ocp-build.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
